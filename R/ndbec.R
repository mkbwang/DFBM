



# ndbec <- function(X, batch=NULL, perc=0.1, max_k=10){
#
#   # X: count matrix with rows as samples and columns as features
#   # batch: vector of batch assignments
#   # perc: percentile chosen from each feature as the threshold
#   # max_k: maximum rank to try for nonnegative binary matrix factorization
#   # returns: denoised and batch-effect corrected matrix
#
#
#
# }

